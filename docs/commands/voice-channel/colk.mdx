import VersionBadge from '../../../src/component/VersionBadge';
import WarningBadge from '../../../src/component/WarningBadge';

# こるくの玉音放送

<VersionBadge>v1.32.0</VersionBadge>
<WarningBadge to={'/docs/dev_docs/running'}>ffmpeg必須機能</WarningBadge>

---

VC 内にこるくの玉音放送を再生するよ。

:::tip

実行者がボイスチャンネルにいないとこのコマンドは安全装置によって封じ込められます。

:::

### `!gyokuon`

こるくの玉音放送を再生するよ。
