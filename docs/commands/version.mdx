import { CommandArgs } from '../../src/component/CommandArgs';
import VersionBadge from '../../src/component/VersionBadge';

# バージョン表示

<CommandArgs versionAvailableFrom="v1.16.0" commandName="version" />

現在のはらちょのバージョンを表示します。

はらちょのバージョンはバグレポートの提出に必要です。
