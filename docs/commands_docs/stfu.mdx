import VersionBadge from '../../src/component/badge/VersionBadge';

# 治安統率機構

<VersionBadge>v1.12.0</VersionBadge>

### `!stfu [count]`

- 第一引数で指定した個数、実行したチャンネルに存在するはらちょのメッセージを最新順から削除します。
  - デフォルト値は `1`
  - 最大で `50` までのメッセージを削除します。
