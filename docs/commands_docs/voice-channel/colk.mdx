import VersionBadge from '../../../src/component/Badge/VersionBadge';
import WarningBadge from '../../../src/component/Badge/WarningBadge';

# こるくの玉音放送

<VersionBadge children={'v1.32.0'} />,
<WarningBadge children={'ffmpeg必須機能'} to={'/docs/dev_docs/running'} />

---

VC 内にこるくの玉音放送を再生するよ。

:::tip

実行者がボイスチャンネルにいないとこのコマンドは安全装置によって封じ込められます。

:::

### `!gyokuon`

こるくの玉音放送を再生するよ。
