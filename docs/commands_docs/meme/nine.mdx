import VersionBadge from '../../../src/component/Badge/VersionBadge';

# 〇〇は〇〇が 9 割

<VersionBadge children={'v1.31.0'} />

## `!nine [arg1] [arg2]`

### Example

`!nine 高専生 単位`

```
>> 高専生は単位が9割
```
